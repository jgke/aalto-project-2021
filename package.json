{
    "scripts": {
        "lint": "eslint . --ext .ts --ext .tsx",
        "prettier": "npx prettier --check .",
        "prettier:write": "npx prettier --write .",
        "build-server": "cd ./src/server && npm i && npm run build && cp -r ./dist/src/server/.  ./../../build/",
        "build-client": "cd ./src/client && npm i && npm run build && cp -r ./build/. ./../../build/dist/",
        "copy-migrations": "cp -r ./src/server/migrations ./build/",
        "install-deps": "cp ./src/server/package.json ./build/ && cp ./src/server/package.json ./build/ && cd build && npm install --production",
        "build": "rm -rf ./build && npm run build-server && npm run build-client && npm run copy-migrations && npm run install-deps",
        "start": "cd build && node index.js",
        "cypress": "cross-env CYPRESS_BASE_URL=http://localhost:3000 cypress run"
    },
    "devDependencies": {
        "@typescript-eslint/eslint-plugin": "^5.4.0",
        "@typescript-eslint/parser": "^5.5.0",
        "cross-env": "^7.0.3",
        "cypress": "^9.1.0",
        "eslint": "^7.32.0",
        "eslint-plugin-import": "^2.25.3",
        "prettier": "2.5.1",
        "supertest": "^6.1.6",
        "typescript": "^4.5.2",
        "use-resize-observer": "^8.0.0"
    },
    "dependencies": {
        "bootstrap": "^5.1.3"
    }
}
