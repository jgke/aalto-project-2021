import {IConfigs} from "./domain/IConfigs";

export const configs: IConfigs = {
    poolConfig: {
        host: 'localhost',
        port: 5432,
        user: 'postgres',
        database: 'postgres',
        password: 'example'
    }
}
